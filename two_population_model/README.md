
## MATLAB code for the two-population model

sim_model_v02.m runs the two population ODE.

The parameters for the ODE system are derived in hiss_fit_*.mat files. **cohort_fits** dir contains the estimated ODE parameters
for the three patient cohorts (BR, TR, Control). 

